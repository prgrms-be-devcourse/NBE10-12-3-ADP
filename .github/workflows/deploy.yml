name: 🚀 배포!

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    name: 1. 도커 이미지 빌드 및 푸시
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Login to GHCR
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: ./back
          platforms: linux/amd64
          push: true
          tags: ghcr.io/prgrms-be-devcourse/nbe10-12-2-adp

  deploy:
    name: 2. EC2에서 도커 이미지 풀 및 컨테이너 실행
    runs-on: ubuntu-latest
    needs: build-and-push

    steps:
      - name: Deploy over SSH
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USERNAME }}
          key: ${{ secrets.EC2_SSH_KEY }}
          port: 22
          script: |
            docker stop application
            docker rm application
            docker rmi ghcr.io/prgrms-be-devcourse/nbe10-12-2-adp
            docker pull ghcr.io/prgrms-be-devcourse/nbe10-12-2-adp
            docker run -d \
              --name application \
              --env-file .env \
              -p 8080:8080 \
              ghcr.io/prgrms-be-devcourse/nbe10-12-2-adp
